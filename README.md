# Synthesizer_with_NumPy
Synthesizer with NumPy functions

  The project is to generate sounds with NumPy math functions. Simple functions like SIN() and COS() could be combined to produce audio signals. NumPy arrays, presenting audio data, are transformed  with diffferent modulations and mastered into tracks.
  
